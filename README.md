# text-to-midi

A short note about the repo.
The evals require access to our model weights and the original text2midi model from
the text2midi github. Along with this it requires access to GigaMidi and MidiCaps, both which were too
big to push into this repo.

Github have us trouble pushing those files since they
exceeded the 50mb limit, 
